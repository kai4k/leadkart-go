// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: persons.sql

package adapters

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getPersonByEmail = `-- name: GetPersonByEmail :one
SELECT id, email, first_name, last_name, password_hash, security_stamp,
       is_active, is_anonymised, created_at, anonymised_at
FROM   identity.persons
WHERE  email = $1
`

func (q *Queries) GetPersonByEmail(ctx context.Context, email string) (IdentityPerson, error) {
	row := q.db.QueryRow(ctx, getPersonByEmail, email)
	var i IdentityPerson
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.FirstName,
		&i.LastName,
		&i.PasswordHash,
		&i.SecurityStamp,
		&i.IsActive,
		&i.IsAnonymised,
		&i.CreatedAt,
		&i.AnonymisedAt,
	)
	return i, err
}

const getPersonByID = `-- name: GetPersonByID :one
SELECT id, email, first_name, last_name, password_hash, security_stamp,
       is_active, is_anonymised, created_at, anonymised_at
FROM   identity.persons
WHERE  id = $1
`

func (q *Queries) GetPersonByID(ctx context.Context, id pgtype.UUID) (IdentityPerson, error) {
	row := q.db.QueryRow(ctx, getPersonByID, id)
	var i IdentityPerson
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.FirstName,
		&i.LastName,
		&i.PasswordHash,
		&i.SecurityStamp,
		&i.IsActive,
		&i.IsAnonymised,
		&i.CreatedAt,
		&i.AnonymisedAt,
	)
	return i, err
}

const insertPerson = `-- name: InsertPerson :exec

INSERT INTO identity.persons (
    id, email, first_name, last_name, password_hash, security_stamp,
    is_active, is_anonymised, created_at
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
`

type InsertPersonParams struct {
	ID            pgtype.UUID
	Email         string
	FirstName     string
	LastName      string
	PasswordHash  string
	SecurityStamp pgtype.UUID
	IsActive      bool
	IsAnonymised  bool
	CreatedAt     pgtype.Timestamptz
}

// Person queries — identity.persons is non-RLS (global identity).
// Email is globally unique; lookup by email is the entry point for login
// + password-reset + email-change flows per multi-tenancy.md "Identity model".
func (q *Queries) InsertPerson(ctx context.Context, arg InsertPersonParams) error {
	_, err := q.db.Exec(ctx, insertPerson,
		arg.ID,
		arg.Email,
		arg.FirstName,
		arg.LastName,
		arg.PasswordHash,
		arg.SecurityStamp,
		arg.IsActive,
		arg.IsAnonymised,
		arg.CreatedAt,
	)
	return err
}

const updatePerson = `-- name: UpdatePerson :exec
UPDATE identity.persons
SET    email          = $2,
       first_name     = $3,
       last_name      = $4,
       password_hash  = $5,
       security_stamp = $6,
       is_active      = $7,
       is_anonymised  = $8,
       anonymised_at  = $9
WHERE  id = $1
`

type UpdatePersonParams struct {
	ID            pgtype.UUID
	Email         string
	FirstName     string
	LastName      string
	PasswordHash  string
	SecurityStamp pgtype.UUID
	IsActive      bool
	IsAnonymised  bool
	AnonymisedAt  pgtype.Timestamptz
}

// General-purpose update covering ChangePassword + Anonymise + future
// mutations. Repository decides which fields actually changed; SQL
// writes whatever the aggregate currently says.
func (q *Queries) UpdatePerson(ctx context.Context, arg UpdatePersonParams) error {
	_, err := q.db.Exec(ctx, updatePerson,
		arg.ID,
		arg.Email,
		arg.FirstName,
		arg.LastName,
		arg.PasswordHash,
		arg.SecurityStamp,
		arg.IsActive,
		arg.IsAnonymised,
		arg.AnonymisedAt,
	)
	return err
}
